export async function applyFeeHistoriesToStudentTermForm(..._args: any[]) {
	return {
		applied: 0,
		skipped: 0,
		total: 0,
		message: "Legacy fee application has been reset and is being rebuilt.",
	};
}
