# QA, Accessibility, And Brain Documentation Pass

Labels: `wayfinder:task`
Status: Open
Blocked by: `005-create-implementation-handoff.md`
Blocks: None

## Question

What final verification and durable documentation updates are required after implementing the student import flat sectioned redesign?

## Context

The redesign changes the operator-facing behavior of the student import flow, so implementation should finish with focused QA and Brain documentation updates. This ticket exists to prevent the design from landing without proof that table scrolling, popovers, menus, mobile layout, and import gating work.

## Resolve

- Define automated checks:
  - parser tests
  - import error tests
  - dashboard typecheck or the narrowest viable check
- Define browser/manual QA:
  - setup screen with empty, valid, warning, and blocked paste data
  - single-classroom and multiple-classroom modes
  - no-match ready rows
  - exact match rows
  - suspected match rows with multiple candidates
  - missing gender
  - missing/ambiguous classroom
  - unchecked blocker rows do not block batch import if approved by ticket 004
  - background job execution/progress/completion
  - mobile/narrow width behavior
- Define accessibility checks:
  - keyboard access to name chips, action dropdown, more menu, match candidate picker
  - focus return after popover/sheet selection
  - screen-reader labels for row checkbox and status
  - no hover-only required interaction
- Define Brain docs to update after code changes:
  - `.brain/features/student-import.md`
  - `.brain/tasks/in-progress.md`, `.brain/tasks/done.md`, or related task tracking only if the implementation starts/finishes under Brain tracking
  - `.brain/api/contracts.md` only if API contracts unexpectedly change

## Expected Answer

A final QA and documentation checklist that the implementation agent can execute before reporting completion.

## Comments

> *This was generated by AI during Wayfinder issue commenting.*

QA should cover setup states, section ordering, checked-row gating, unresolved gender/classroom rows, exact matches, suspected matches with multiple candidates, action changes, match candidate selection, async job progress, completion, and mobile layout. Accessibility must verify keyboard access for name chips, action dropdown, more menu, candidate picker, row checkboxes, and focus return. After implementation, update `.brain/features/student-import.md`; update API docs only if contracts changed.
