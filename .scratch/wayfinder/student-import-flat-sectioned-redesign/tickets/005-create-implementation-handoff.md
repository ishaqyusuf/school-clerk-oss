# Create Implementation Handoff

Labels: `wayfinder:task`
Status: Open
Blocked by: `001-define-minimal-setup-screen-contract.md`, `002-design-sectioned-review-table-model.md`, `003-define-row-interaction-patterns.md`, `004-define-footer-counts-and-import-gating.md`
Blocks: `006-qa-accessibility-and-brain-documentation-pass.md`

## Question

What exact implementation plan should an agent follow to build the approved flat setup screen and sectioned table review without changing import business contracts?

## Context

Once the UI and gating decisions are resolved, the next agent needs a file-level plan. The current likely files are `index.tsx` for setup and `import-activities.tsx` for review. The existing component is large, so the handoff should recommend safe extraction boundaries instead of a risky one-shot rewrite.

## Resolve

- Summarize the approved decisions from tickets 001-004.
- Identify files/components to edit or extract.
- Define a low-risk implementation sequence:
  - setup screen simplification
  - review layout state selectors
  - sectioned table shell
  - row cells and popovers/menus
  - footer gating
  - execution/completion preservation
- Identify existing helpers to preserve or move:
  - row decision defaults
  - `buildExecuteRow` behavior
  - candidate helpers
  - name split helpers
  - gender/classroom resolution helpers
  - error normalization
  - job polling/invalidation
- Define required tests and manual QA steps.
- Define Brain documentation updates required after implementation.

## Expected Answer

An implementation-ready handoff file with ordered steps, acceptance criteria, test plan, and Brain update requirements.

## Comments

> *This was generated by AI during Wayfinder issue commenting.*

The handoff should preserve existing parser, verification, execution, async job, and invalidation contracts. Recommend extracting selectors/helpers before reshaping JSX: row sectioning, row readiness, footer counts, candidate summary, and action validity. Then implement in order: setup simplification, sectioned table shell, row cells/menus/popovers, footer gating, and execution/completion preservation. Avoid API/schema changes unless a hard blocker appears.
